# Repo root — used by Docker Compose for frontend build arg only.
# Backend config lives in apps/backend/.env

# Production aaPanel: leave empty (same-origin /api proxy)
# Local Docker build: https://your-domain.com
NEXT_PUBLIC_API_URL=
