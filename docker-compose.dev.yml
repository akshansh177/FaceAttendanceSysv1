# Local development — Redis only (MySQL uses apps/backend/.env).
# Usage: docker compose -f docker-compose.dev.yml up -d

services:
  redis:
    image: redis:7-alpine
    ports:
      - "127.0.0.1:6379:6379"
    restart: unless-stopped
